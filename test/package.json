{
    "name": "snarkjs-verification-tests",
    "version": "1.0.0",
    "description": "Verification tests for gnark-to-snarkjs exported files",
    "type": "module",
    "scripts": {
        "test": "vitest run"
    },
    "devDependencies": {
        "@types/node": "^25.6.0",
        "@types/snarkjs": "^0.7.9",
        "snarkjs": "^0.7.0",
        "typescript": "^5.0.0",
        "vitest": "^1.0.0"
    },
    "dependencies": {
        "cross-env": "^10.1.0"
    }
}
